# Services Directory

This directory will contain API clients and data service abstractions for connecting the frontend visualization layer to backend services, model prediction outputs, and data providers (e.g. IceNet-MP forecasting models, ERA5/OSI-SAF data pipelines, and route planning services).
